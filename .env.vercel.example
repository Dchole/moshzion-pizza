# Vercel Production Environment Variables (Prisma 7)
# These are automatically created when you add Vercel Postgres Storage

# Database - Vercel automatically creates these when you add Postgres:
# DATABASE_URL - Direct connection (auto-created by Vercel)
# POSTGRES_URL - Same as DATABASE_URL (auto-created by Vercel)
# PRISMA_DATABASE_URL - Prisma Accelerate URL (optional)

# ✅ No need to add DATABASE_URL - Vercel creates it automatically!

# You only need to add these manually:

# NextAuth - IMPORTANT: Generate new secret for production!
# Generate with: openssl rand -base64 32
AUTH_SECRET="YOUR_PRODUCTION_SECRET_HERE"
NEXTAUTH_URL="https://your-domain.vercel.app"

# OAuth Providers (Optional)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# SMS Provider (Production - Hubtel)
# Sign up at https://hubtel.com and get your credentials
# Pricing: ~$0.003-0.006 per SMS (cheapest for Ghana)
HUBTEL_CLIENT_ID="your-hubtel-client-id"
HUBTEL_CLIENT_SECRET="your-hubtel-client-secret"
HUBTEL_SENDER_ID="Moshzion"
